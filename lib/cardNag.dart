import 'package:flutter/material.dart';

class cardnag extends StatelessWidget {
  const cardnag({super.key});

  @override
  Widget build(BuildContext context) {
    return Material(
      child: Text('what the fuck am i doing here????'),
    );
  }
}
